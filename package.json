{
  "name": "directory-maker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "concurrently \"npm run start --prefix frontend\" \"npm run dev --prefix backend\"",
    "build": "echo 'Starting build' && cd frontend && npm install && echo 'Frontend dependencies installed' && npm run build && echo 'Frontend build complete' && cd .. && echo 'Moving build folder' && mv frontend/build . && echo 'Build folder moved' && ls -la",
    "vercel-build": "npm run build"
  },
  "dependencies": {
    "@octokit/rest": "^21.0.1",
    "@vercel/edge-config": "^1.2.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "micro": "^10.0.1",
    "stripe": "^16.7.0",
    "uuid": "^10.0.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}